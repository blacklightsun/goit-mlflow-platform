# 02-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
  namespace: minio
type: Opaque
data:
  # echo -n 'YOUR_ACCESS_KEY' | base64
  accessKey: minio # Замініть на ваш закодований ключ
  # echo -n 'YOUR_SECRET_KEY' | base64
  secretKey: minio123 # Замініть на ваш закодований пароль